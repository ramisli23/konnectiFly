/*import 'package:konnecti/modules/package_pricing.dart';

class CountryPackage {
  final String name;
  final String code;
  final List<Packagepricng> packages;

  CountryPackage({
    required this.code,
    required this.name,
    required this.packages,
  });

  factory CountryPackage.fromJson(Map<String, dynamic> json) {
    return CountryPackage(
      name: json['name'] ?? '',
      code: json['code'] ?? '',
      packages: (json['packages'] as List)
          .map((pkg) => Packagepricng.fromJson(pkg))
          .toList(),
    );
  }
}*/
